# wattoeat

Backend for wattoeat mobile app, that helps couples decide what to eat.

## Instructions

1. `npm install`
1. `cp .env.sample .env` and fill in environment variables
1. To initialize SQLite db: `npm run migrate-db && npm run seed-db`
1. To run tests: `npm test`
1. To start the API server: `npm start`